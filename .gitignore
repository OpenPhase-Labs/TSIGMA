.vscode/
.antigravity/
__pycache__/

# Every .env variant is a secret-bearing file — only the template is committed.
.env*
!.env.example
.venv
venv

.coverage
htmlcov/
.pytest_cache/

# Python build / type-check artifacts
*.egg-info/
build/
dist/
.mypy_cache/

# Agent workspace directories
plans/
specs/
issues/
.claude/
.zoocode/
.tdd-bridge.log

.ruff_cache/

# MCP cache
.playwright-mcp/

# Local CSS build tools (Tailwind standalone binary — fetched per-dev, never committed)
tools/

# Per-agency white-label themes are deployment artifacts, not open-core —
# except the committed `example` template agencies copy from.
themes/*
!themes/example/
