.vscode/
.antigravity/
__pycache__/

.env
.venv
venv

.coverage
htmlcov/
.pytest_cache/

# Agent workspace directories
plans/
specs/
.claude/
.zoocode/

# MCP cache
.playwright-mcp/

# Local CSS build tools (Tailwind standalone binary — fetched per-dev, never committed)
tools/

# Per-agency white-label themes are deployment artifacts, not open-core —
# except the committed `example` template agencies copy from.
themes/*
!themes/example/